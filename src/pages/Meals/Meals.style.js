/* eslint-disable prettier/prettier */
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#fada5e', // koyu sarı renk
    },
    listContainer: { // Bu stil'i ekledim
        padding: 10, // Üstten biraz boşluk eklemek için
    },
});

export default styles;